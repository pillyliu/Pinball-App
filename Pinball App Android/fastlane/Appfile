package_name("com.pillyliu.pinballandroid")

# Set this to your Play Console service account JSON key.
# You can override with SUPPLY_JSON_KEY at runtime.
json_key_file(ENV["SUPPLY_JSON_KEY"] || "fastlane/play-store-service-account.json")
